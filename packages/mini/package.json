{
  "name": "@cloud-dock/mini",
  "version": "1.0.0",
  "description": "CloudDock Mini Program (微信小程序)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "minig-cli dev",
    "build": "minig-cli build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "dependencies": {
    "@cloud-dock/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/react": "^18.3.0"
  }
}
