import { WSMessage } from '@cloud-dock/shared';
import { WSClientMessageType, WSServerMessageType } from '@cloud-dock/shared';

export type {
  WSMessage,
  WSClientMessageType,
  WSServerMessageType,
  WSAuthSuccess,
  WSAuthError,
  WSHeartbeat,
  WSTunnelOpen,
  WSTunnelOpenAck,
  WSTunnelData,
  WSBindRequest,
  WSBindConfirm,
  WSBindConfirmAck,
} from '@cloud-dock/shared';
